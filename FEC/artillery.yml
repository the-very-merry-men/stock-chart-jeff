config:
  target: 'http://localhost:3001'
  phases:
    - duration: 60
      arrivalRate: 100
    - duration: 120
      arrivalRate: 300
    - duration: 60
      arrivalRate: 500
 
scenarios:
  - name: 'Stock increment search'
    flow:
    - get:
        url: "/stocks/{{ $randomNumber(1,10000000) }}"